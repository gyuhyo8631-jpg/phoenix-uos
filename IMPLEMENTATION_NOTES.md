# 사진·길안내 운영 반영 기록

운영 도메인 `korea.phoenix-uos.com`은 Vercel 프로젝트 `best-esim-korea-2026-2`와 GitHub 저장소 `gyuhyo8631-jpg/korea-easy`의 `main` 브랜치에 연결되어 있다. 운영 페이지는 루트 `index.html`의 단일 정적 문서로 구성되어 있으며, 사진 명소·음식점·인기 명소 데이터와 렌더링 함수가 같은 파일에 포함되어 있다.

수정본은 N서울타워, 명동성당, 서울로7017 사진을 WebP로 최적화해 `assets/`에 추가했다. 음식점·사진 명소·인기 명소·역 지도 버튼은 카카오맵, 네이버지도, Google Maps 중 하나를 선택하는 길안내 창을 열며, 대표 세 장소는 위도·경도 기반 목적지를 사용한다. 그 밖의 장소는 장소명과 서울 키워드로 지도 앱 검색 또는 길안내를 연다.

로컬 정적 서버에서 페이지가 정상 렌더링되고 기존 다국어 콘텐츠 및 카드 데이터가 유지되는 것을 확인했다. JavaScript 문법, 필수 지도 선택 요소, 사진 자산, 세 지도 제공자 URL은 자동 검증을 통과했다.

데스크톱 브라우저에서 사진 명소 섹션의 N서울타워·명동성당·서울로7017 이미지가 각 카드 상단에 표시되는 것을 확인했다. N서울타워의 `Directions` 버튼은 지도 앱 선택창을 열며, 카카오맵·네이버지도·Google Maps 링크 모두 `37.55117, 126.988228` 목적지 좌표를 포함한다. 모바일 390×844 화면에서도 로고, 언어 전환, 히어로 문구와 빠른 메뉴가 가로 넘침 없이 한 열로 표시된다.
